import { ButtonGroup, Button } from 'react-bootstrap';
import React from 'react';

// const EventFilters = ({ onFilter }) => (
//   <ButtonGroup>
//     <Button onClick={() => onFilter('city')}>Filtra per città</Button>
//     <Button onClick={() => onFilter('venue')}>Filtra per luogo</Button>
//     <Button onClick={() => onFilter('type')}>Filtra per tipo</Button>
//     <Button onClick={() => onFilter('search')}>Inizia ricerca</Button>
//   </ButtonGroup>
// );

const EventFilters = () => <div> EventFilters Placeholder</div>

export default EventFilters;
